# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=restaurante
DB_USER=restaurante
DB_PASSWORD=restaurante123

# JWT (gere uma chave segura em produção: openssl rand -base64 32)
JWT_SECRET=dev-secret-key-change-in-production-min-32-chars-base64-encoded
JWT_EXPIRATION=86400000

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Upload
UPLOAD_DIR=./uploads