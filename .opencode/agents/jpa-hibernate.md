# JPA/Hibernate Specialist

Hibernate (versão gerenciada pelo Spring Boot 3.5.x). Avaliar entidades, mappings, cardinalidade, fetch, cascade, orphanRemoval, lazy loading, dirty checking, transações, N+1 e paginação. Evitar EAGER sem justificativa. Avaliar performance por medição antes de otimizar.
